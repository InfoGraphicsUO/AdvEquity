# Mapping AdvancED Equity Project

## Project Overview

This is a proof-of-concept interactive educational equity dashboard that builds upon ProPublica's "Miseducation" website/dashboard. It incorporates more recent data and longitudinal trends across multiple years. The dashboard focuses on educational equity metrics such as AP (Advanced Placement) course opportunities, racial composition, economic disadvantage, student-teacher ratios, and more.

Key features:
- Interactive map for exploring AP opportunity estimates by district and state.
- Data visualizations for students, teachers, and resources.
- Focus on disparities for Black and Hispanic students compared to White students.

The project uses HTML, CSS, JavaScript (with libraries like Plotly and Mapbox GL JS), and Python (via Jupyter Notebook for data processing). It currently uses dummy data in some places (e.g., opportunity estimates).

**Target Audience:** Educational researchers, policymakers, and equity advocates.

**Current Status:** Proof-of-concept. Data is partially dummy; real data integration is ongoing (e.g., from JSON files like AllStates.json).

**Dependencies:**
- Mapbox GL JS (requires a Mapbox access token: `MAPBOXTOKEN` in map.js).
- Plotly.js for charts.
- Python libraries (for data processing): pandas, numpy, json, matplotlib.
- No internet access for code execution in some tools (as per guidelines).
- Fonts: jaf-bernina-sans (assumed to be available via CSS).

**Setup Instructions:**
1. Clone the repository.
2. Place assets (e.g., GeoJSON files like oregon_districts.geojson, JSON data like AllStates.json) in `/assets/data/`.
3. Set your Mapbox access token in `map.js`.
4. Open `map.html` in a browser to view the dashboard.
5. For data processing, run the Jupyter Notebook `adv_equity_data_to_json.ipynb` in a Python environment.
6. Ensure CSS variables (e.g., colors) are consistent across files.

**Running the Project:**
- The main entry point is `map.html`, which embeds or links to the map and charts.
- TODO make an entrypoint that is not the iframe map.html
- Use a local server for development to avoid CORS issues with local files.

**Known Issues/Limitations:**
- Some data is dummy (e.g., urban scores, opportunity estimates in district data).
- Early data (2011–12) may be unreliable;
- High school filtering needs refinement (e.g., distinguish K-12 schools).
- No additional package installations allowed in code execution environments.
- State level table only shows recent AP values and opp estimate for Black Students. 


## File Structure and Contents

Below is a detailed overview of each file in the project, including key code snippets, purposes, and extracted information.

### index.html
This is the main HTML file for the dashboard landing page.

- **Purpose:** Displays the project title, description, and embeds the interactive map/dashboard.
- **Key Content:**
  ```html
  <h1>Mapping AdvancED Equity</h1>
  <p>Below you will find a proof-of-concept interactive educational equity dashboard that builds upon ProPublica's "Miseducation" website/dashboard while incorporating more recent data and longitudinal trends across multiple years.</p>
  <!-- Embeds map or other components here -->
  ```

### charts.js
JavaScript file for initializing and rendering longitudinal charts using Plotly.

- **Purpose:** Creates trend charts for metrics like % non-white students, % economically disadvantaged, % HS students taking AP, student-teacher ratio, and modal AP courses per school. Uses dummy data for Oregon.
- **Key Functions:**
  - `initGraph()`: Sets up the HTML structure for charts and legend.
  - `createLongitudinalChart(containerId, values, stateAvg)`: Plots line charts with markers and a dashed state average line.
  - `initStateOverview()`: Creates a pie chart for racial composition (dummy data: Two-or-more: 6.1%, Asian: 4.5%, Black: 1.9%, Hispanic: 13.9%, White: 71.7%).
- **Configuration:**
  - Years: [2011, 2014, 2017, 2020, 2022]
  - Colors: Black for trends, Yellow for state average.
  - Example Calls (Dummy Data):
    ```javascript
    createLongitudinalChart('chart1', [45, 47, 50, 52, 55], 50);  // % non-white students
    createLongitudinalChart('chart2', [60, 61, 63, 65, 68], 63);  // % economically disadvantaged
    // ... (similar for others)
    ```
- **HTML Generated by initGraph():**
  - Includes a legend, chart captions, and containers like `#chart1` to `#chart5`.

### map.html
HTML structure for the interactive map view.

- **Purpose:** Renders the map interface with legends, search, buttons, and info panels.
- **Key Elements:**
  - Map title: "AP Opportunity estimate" (with note: *dummy data).
  - Legend: Color-coded opportunity levels (1-5, N/A).
  - Buttons: "Go" (search), "View US", "Explore Districts".
  - Race Selection: Toggle between Black and Hispanic students.
  - Info Panel: Description of US-level data, table for state-level opportunity estimates.
  - Gradient Legend: Low to High opportunity.
  - Table Structure:
    ```html
    <table>
      <tr><th>State</th><th>Model Ap Classes</th><th>Opp Est 2011</th><th>Opp Est 2021</th></tr>
      <!-- Rows for states -->
    </table>

    table.row.add([stateAbbrev, ap, val2011, val2021]) add rows by calling data tables api
    ```
  - Dummy Text: "Info about the US. Lorem ipsum..."

### index.js
Empty or minimal JavaScript file.

- **Purpose:** Likely intended for main app logic, but currently empty.
- **Content:**
  ```javascript
  // Empty file
  ```

### map.js
Core JavaScript file for map functionality using Mapbox GL JS.

- **Purpose:** Handles map initialization, event listeners, data fetching, layer rendering, popups, and interactions.
- **Key Features:**
  - Event Listeners: Search button, full extent (zoom to US), race toggle (Black/Hispanic gaps).
  - CSS Variables: Fetches colors like `--verydarkgrey`, `--green`, etc.
  - Map Setup:
    ```javascript
    mapboxgl.accessToken = MAPBOXTOKEN;
    const map = new mapboxgl.Map({
      container: 'map',
      style: 'mapbox://styles/mapbox/dark-v11',
      bounds: [[-126, 24], [-66, 50]],  // US bounds
      // ... other options
    });
    ```
  - Sources: States GeoJSON, Oregon districts GeoJSON, All districts vector tileset.
  - Layers: State fills/lines (colored by opportunity gaps), district fills/lines (with hover).
  - Interactions: Hover for popups, click to zoom/fly to states/districts, highlight tables.
  - Data Fetching: GeoJSON for states, JSON for state data (e.g., `AllStates.json`).
  - Functions:
    - `fillMap(map, geojson, stateData, fieldName)`: Colors map based on fields like 'ENR_AP_GAP_BL' (Black gap).
    - `buildOpportunityTable(geojson, stateData)`: Builds state table (stubbed; uses DataTables in full).
    - `fillDistricts(map, ...)`: Colors districts with dummy urban scores.
    - `showGraphs()` / `hideGraphs()`: Toggles info/graph containers.
- **Data Notes:** Uses FIPS codes, sorts Oregon first in tables.

### charts.css
CSS for styling charts and graphs.

- **Purpose:** Styles Plotly charts, legends, and containers.
- **Key Styles:**
  - `.chart-row`: Flex layout for multiple charts.
  - `.chart-block`: Individual chart wrappers (flex: 1 1 18%).
  - `.legend-row`: Flex for legend items.
  - Colors: Uses variables like `--almostBlack`, `--yellow`.
  - Heights: Charts at 150px.

### style.css
Global CSS styles for the site.

- **Purpose:** Defines root variables, body styles, headers, and main container.
- **Root Variables:**
  ```css
  :root {
    --almostBlack: #303333;
    --lightgrey: #bdbfbf;
    --green: #808c73;
    --yellow: #e5de54;
    /* ... */
  }
  ```
- **Key Styles:**
  - Body: Background rgb(141,141,142), font: jaf-bernina-sans.
  - Header: Flex with images, height 130px.
  - Main Container: Centered, 80% width.
  - iFrame: Full-width embed for map.

### mapStyle.css
CSS specific to the map interface.

- **Purpose:** Styles map widgets, legends, popups, and overlays.
- **Root Variables:** Similar to style.css, plus `--darkGreen`, `--offwhite`.
- **Key Styles:**
  - `#map`: Full height minus header.
  - Widgets: Positioned absolute (e.g., search top-right).
  - Toggle Buttons: Pill-shaped for race selection.
  - Popup: Custom fonts/sizes.
  - Legend: Gradient bar, color swatches.
  - State Overview: Sidebar panel (hidden initially).

### table.css
CSS for the opportunity table.

- **Purpose:** Styles the state opportunity table as a flex-based grid.
- **Key Styles:**
  - `.row`: Flex rows with borders.
  - Columns: Fixed widths (e.g., state 25%).
  - Highlight: `--yellow` background.
  - Responsive: Column layout on small screens.

### adv_equity_data_to_json.ipynb
Jupyter Notebook for data processing and analysis.

- **Purpose:** Processes educational data (e.g., AP enrollment, racial gaps) into JSON. Includes fact sheet calculations and visualizations.
- **Dependencies:** pandas, numpy, json, matplotlib.
- **Key Sections:**
  - Action Items: List of todos (e.g., filter high schools, decide on metrics).
  - New Data Update: Notes on using total enrollment as denominator initially.
  - Fact Sheet Function:
    ```python
    def fact_sheet(district_data):
        # Calculates % non-white, % EDS, % HS in AP, student-teacher ratio, modal AP courses.
        # Plots donut charts for key metrics.
    ```
  - Data Handling: Aggregates enrollment, rates, etc.
  - Visualizations: Matplotlib pie/donut charts.
- **Notes from Notebook:**
  - Use high school enrollment for K-12 schools.
  - Opportunity Estimates: Fields like APLIKELIHOOD_BL (Black), _HI (Hispanic), _AS (Asian).
  - Decisions Needed: Aggregation methods, thresholds, school inclusion criteria.

This README compiles all provided file contents. For questions, review the source files, run the project locally or contact Izak.Boardman@gmail.com